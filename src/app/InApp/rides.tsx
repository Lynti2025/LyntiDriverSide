import { View, Text } from "react-native";

const Rides = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Hello from Rides!</Text>
    </View>
  );
};

export default Rides;
